# 视图与布局

## 视图模式

三种文件浏览模式：

- **网格视图** — 卡片式排列，适合浏览图片
- **自适应视图** — 根据容器宽度自动调整卡片大小
- **列表视图** — 紧凑的表格排列，显示更多元数据

## 缩放

每个视图模式独立记忆缩放级别。通过缩放滑块或快捷键调整卡片/行的大小。

## 卡片信息

在文件卡片上可以切换显示：

- 文件名
- 文件类型
- 文件大小
- 图片尺寸

## 面板布局

侧边栏和详情面板都支持拖拽调整宽度，调整后自动保存。
