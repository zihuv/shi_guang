# AI 批量分析

选中一批文件，让 AI 自动为它们生成标签和描述。

## 配置

打开设置 → AI → 批量分析：

- **服务端点** — 配置外部 AI 服务的地址和 API 密钥。支持配置多个目标
- **并发数** — 同时处理的文件数量
- **自动分析** — 开启后，新导入的文件会自动触发 AI 分析

## 使用

1. 在素材页选中需要分析的文件
2. 右键菜单选择"AI 分析"
3. 分析结果会自动填充到文件的标签和描述字段

## 测试连接

配置服务端点后，可以在设置页点击"测试连接"，确认服务可用。
